{
  "name": "amadal",
  "version": "1.0.0",
  "private": true,
  "description": "Amadal — Shopify Online Store 2.0 theme",
  "scripts": {
    "dev": "shopify theme dev",
    "push": "shopify theme push",
    "pull": "shopify theme pull",
    "check": "shopify theme check",
    "share": "shopify theme share"
  },
  "devDependencies": {
    "@shopify/cli": "^3.0.0",
    "@shopify/theme": "^3.0.0"
  }
}
